import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'khd.proyects.contacts',
  appName: 'My Contacts',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
