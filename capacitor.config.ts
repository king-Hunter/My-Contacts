import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'kh.project.contacts',
  appName: 'Mis Contactos',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
